var writeCSS = require('./write-css').writeCSS;
var sass = require('node-sass');

function render(inputPath, outputPath, content, createMappings) {
  // use the sass api -- make sure there's a sourcemap
}

module.exports = render;
