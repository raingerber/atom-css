function FileSelection(path, range, text) {
  this.path = path;
  this.range = range;
  this.text = text;
}

FileSelection.prototype.getAtomLine;

FileSelection.prototype;

module.exports = FileSelection;
